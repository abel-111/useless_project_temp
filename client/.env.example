# Client Environment Variables
# Leave blank when using same-origin or reverse-proxy rewrites.
# When deploying client separately from server (e.g. Vercel frontend + Render backend),
# set VITE_API_URL to your deployed backend URL (e.g. https://ghost-detector-api.onrender.com)
VITE_API_URL=
